# Copy this file to .env and fill in your values.
# .env is git-ignored – never commit it.

# ── HuggingFace ─────────────────────────────────────────────────────
# Required for gated models (LLaMA, Mistral). Generate at:
# https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# ── Azure ML workspace ──────────────────────────────────────────────
AML_SUBSCRIPTION_ID=aml subscrition id here
AML_RESOURCE_GROUP=aml resource group here
AML_WORKSPACE_NAME=aml workspace name here
AML_COMPUTE_CLUSTER=aml compute cluster name here